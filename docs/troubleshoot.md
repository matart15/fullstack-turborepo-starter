# troubles

## 1. when hasura fails to load meta data

![hasura meta data fail](/docs/files/hasura_metadata_fail.png)

- make sure NestJs server is running without problem (http://localhost:5000/graphql)
- click reload metadata
